// Vérification que l'appareil n'est pas déjà listé dans les tags 

function checkIfTagIsAlreadyChecked(tag, type) {

    if (isTagAlreadyChecked = displayedTagsParams[type].includes(tag)) {
        return true
    }

}